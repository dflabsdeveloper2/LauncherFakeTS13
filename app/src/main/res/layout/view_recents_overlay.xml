<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/recents_content"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_popup_drawer"
    android:elevation="12dp">

    <!-- Handle pill -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="8dp"
        android:background="@color/drawer_handle"
        android:alpha="0.4" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginBottom="10dp"
        android:text="@string/recent_apps"
        android:textColor="@color/drawer_text"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- Apps grid/list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_recents_apps"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingHorizontal="8dp"
        android:paddingBottom="8dp"
        android:clipToPadding="false" />

    <!-- Clear all slider -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center_horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/swipe_to_close_all"
            android:textColor="@color/drawer_text"
            android:textSize="12sp"
            android:alpha="0.7"
            android:layout_marginBottom="4dp" />

        <SeekBar
            android:id="@+id/sb_clear_all"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="100"
            android:progress="0"
            android:thumb="@drawable/ic_seekbar_thumb"
            android:splitTrack="false" />

    </LinearLayout>

</LinearLayout>
