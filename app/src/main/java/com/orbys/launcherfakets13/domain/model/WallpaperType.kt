package com.orbys.launcherfakets13.domain.model

/**
 * Representa el tipo de fondo de pantalla activo.
 */
enum class WallpaperType {
    /** Fondo de pantalla predefinido por el entorno/perfil. */
    DEFAULT,
    /** Fondo de pantalla personalizado por el usuario a través del sistema. */
    CUSTOM
}
